import { Helmet } from "react-helmet-async";

const MyListings = () => {
  return (
    <section>
      <Helmet>
        <title>My Listings | Dashboard</title>
      </Helmet>

      {/* my listing */}
    </section>
  );
};

export default MyListings;